spring:
  application:
    name: tripium_test_task
  datasource:
    url: jdbc:postgresql://localhost:5432/tripium_test_task
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  main:
    web-application-type: NONE